# 组件开发进度（队列）

> 说明：用于跟踪组件开发状态。  
> 状态建议：`未开始` / `进行中` / `已完成` / `已阻塞`

## 当前未完成组件

| 组件 | 当前状态 | 备注 |
| --- | --- | --- |
| 普通按钮组件 | 未开始 | 待实现基础按钮能力与文档示例 |
| 标签组件 | 未开始 | 待确认视觉样式与交互规范 |
| 通用 Pick 组件 | 未开始 | 待明确单选/多选、弹层与数据结构 |
| 单选组件 | 未开始 | 待实现受控/非受控模式与事件 |
| Input 组件 | 未开始 | 待补齐统一规范版本（属性/事件/样式变量） |

## 使用说明

- 每次组件状态变化后，及时更新本表。
- 组件完成后，同步补齐：
  - 组件实现文件（`*.ts`）
  - 测试文件（`*.spec.ts`）
  - Storybook 文档（`*.stories.ts`）

- 重点：最后的包体积优化



事件可以直接input绑定原生的事件和属性，这里只做样式的封装

选择组件 yn-group-pick 和 yn-pick 搭配使用

父组件叫yn-group-pick

value属性：可以接收数组和number或者string，进行回显

change事件：(ids: 选中的id集合)=>{} 因为有的时候是多选这里回传类型直接就定义成函数

multiple: true的时候可以选择多个yn-pick

selected-icon: 选中的图标，这个默认会有一个图标在上面截图里

close-icon:取消图标，这个默认没有不传的话取消就没有图标显示

子组件 yn-pick

就是一个div