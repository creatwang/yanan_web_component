# 组件开发进度（队列）

> 说明：用于跟踪组件开发状态。  
> 状态建议：`未开始` / `进行中` / `已完成` / `已阻塞`

## 当前未完成组件

| 组件        | 当前状态 | 备注 |
|-----------|------| --- |
| 普通按钮组件    | 已完成   | 已完成属性/事件/插槽/样式变量/Storybook/测试同步 |
| 标签组件      | 未开始  | 待确认视觉样式与交互规范 |
| 通用 Pick 组件 | 未开始  | 待明确单选/多选、弹层与数据结构 |
| Input 组件  | 未开始  | 待补齐统一规范版本（属性/事件/样式变量） |
| 下拉组件      | 未开始  | 待补齐统一规范版本（属性/事件/样式变量） |
- 组件名称后面重新命名一下
## 使用说明

- 每次组件状态变化后，及时更新本表。
- 组件完成后，同步补齐：
  - 组件实现文件（`*.ts`）
  - 测试文件（`*.spec.ts`）
  - Storybook 文档（`*.stories.ts`）

- 重点：最后的包体积优化

## `yn-button` 已交付能力（2026-05）

- 语义色：`variant="primary|success|warning|danger|neutral|dark|default"`
- 尺寸：`size="mini|small|medium"`（默认 `medium`）
- 事件：使用原生 `click`
- 文案：使用默认插槽（`<slot></slot>`，支持自定义类名）
- 图标插槽：`prefix-icon` / `suffix-icon`
- Loading：
  - `loading`、`loading-type="left|center|right"`
  - `loading-type="center"` 时保留文本且 loading 层整体居中
  - 支持 `slot="loading"` 自定义 loading（动画由使用方自行控制）
- 命中热区：`hit-slop`（默认开启，`::before` 外扩 5px）
- 禁用态：`disabled`（按 `variant` 匹配禁用配色）
- 背景层：使用内部绝对定位 `span.bg`
- 圆角：通过 `--yn-button-radius` 自定义
- 常用 CSS 变量：`--yn-button-bg`、`--yn-button-hover-bg`、`--yn-button-disabled-bg`、`--yn-button-disabled-color`、`--yn-button-radius`、`--yn-button-loading-size`



选择组件有2个组件搭配使用
父组件：yn-group-pick

value属性：可以接收数组和number或者string，进行回显

change事件：(ids: 选中的id集合， flag: boolean 是否选中)=>{} 因为有的时候是多选这里回传类型直接就定义成函数

multiple: 默认false只能选中一个yn-pick, true的时候可以选择多个yn-pick

selected-icon: 选中状态图标。`selected=true` 时展示。

unselected-icon:未选中状态图标，默认是描边圆环图标，不传会使用默认值。

show-unselected-icon:未选中状态是否展示 unselected-icon，默认 false。


子组件：yn-pick

子组件 yn-pick，点击就触发yn-group-pick的change事件

然后子组件默认插槽会塞入到这个相对div里面，由用户自定义内容

就是一个相对定位 div，命名为 wrap，包裹右上角图标：选中时显示 selected-icon；未选中时仅在 show-unselected-icon=true 时显示 unselected-icon。

border属性：默认值true, wrap 会有一after伪类，并且pointer-events: none，他会设置1px，#000边框，8px圆角,覆盖在默认插槽的元素上

自定义变量控制  border-width border-color 还有圆角

这个是默认选中的图标
<svg data-v-410be210="" width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
<path data-v-410be210="" d="M10 18C14.4182 18 18 14.4182 18 10C18 5.58172 14.4182 2 10 2C5.58172 2 2 5.58172 2 10C2 14.4182 5.58172 18 10 18Z" fill="#241F21"/>
<path data-v-410be210="" fill-rule="evenodd" clip-rule="evenodd" d="M14.1207 8.02566L8.86034 13.0355L6 10.3114L7.03448 9.22517L8.17069 10.3073C8.5569 10.6751 9.16379 10.6751 9.55 10.3073L13.0862 6.93945L14.1207 8.02566Z" fill="white"/>
</svg>


还有一种下拉模式就是点击按钮打开下拉内容的时候可以指定按钮的颜色和文本颜色，
然后这个下拉是一个yn-group-pick，选中pick item的时候会关闭下拉，
然后将选中的node，回传到button组件里，看能不能使用作用域插槽，
在button组件里决定要展示哪个字段，
当内容区域关闭了之后，按钮背景和文本也就都恢复原样了

1. hover 展示的时候要有动画
2. 默认按钮太大了尺寸统一缩小
3. 添加top属性，可以设置top距离 
4. 双插槽情况下，元素没有紧挨着绳子，另外拉动没有效果
4. header遮盖的问题
5. 调整size

# 支付组件
以下是个人软件出海，通过 Lemon Squeezy（代收单 https://www.lemonsqueezy.com/） ➡️ 网页/App（前端集成） ➡️ 万里汇/PayPal（个人提现） 的完整对接保姆级教程。


# 购物车调用接口
你的担忧是 100% 正确的：绝对不能在用户“增加购物车”时去扣减数据库里的真实库存。 
如果按照购物车来扣库存，恶意用户只要把 100 件商品加进购物车却不去付钱，
你的商品就会直接显示“售罄”，导致真正想买的人买不到，这叫“恶意占位”。
所以，那些大厂独立站在“增加购物车数量时调用后端接口”，
并不是为了“扣除库存”，而是为了“校验（Check）当前瞬间的库存上限，并做好结算准备”。
电商系统标准的“三段式”库存管理逻辑是这样用代码实现的：
第一阶段：加入/增加购物车（只校验，不扣减）当用户在购物车把数量从 1 变成 3，
调用后端接口时，后端执行的代码逻辑是：
核心动作：仅仅是去数据库看一眼：当前库存量是否 ＞＝ 3。
处理结果：如果库存够，后端返回 成功，允许用户在前端把数字变成
3。此时数据库里的库存总量数字完全没有变，别人依然可以继续加购物车。
如果库存不够（比如只剩 2 个），后端直接拒绝，并提示用户“抱歉，
该定制品目前最多只能制作 2 件”。
第二阶段：点击结账 ➡️ 唤起 Google Pay（预扣/预锁定库存）当用户点击
“使用 Google Pay 购买”，准备刷脸付钱的一瞬间，前端会向后端发送请求。
此时，后端才会真正开始干预库存：核心动作（Redis 预扣）：
为了防止两个人在同一秒钟一起刷脸买走最后一件衣服，
后端会在高性能的 Redis 缓存里执行减库存操作（比如 DECRBY product_stock 3），
把这 3 件商品暂时冻结（Lock）起来。设置过期时间：
在代码里，这个冻结状态通常只有 5 - 15 分钟 的有效期。如果用户刷脸成功，
顺利付了钱，进入第三阶段。如果用户在弹出 Google Pay 界面后犹豫了，
最后点击了取消，或者由于信用卡额度不足付款失败。一旦超过 15 分钟，
后端的定时任务（Cron Job）或 Redis 键过期事件会自动把这 3 个库存释放，
原路退回库存池。第三阶段：Lemon Squeezy / 空中云汇 Webhook 异步回调（真正核销库存）当你的后端服务器收到支付平台发来的 order_created（支付成功） Webhook 通知时 [^1]：核心动作（正式扣减）：后端终于百分之百确定这笔钱已经稳妥落入口袋。此时，后端才会真正向 MySQL 数据库发送最终的 SQL 命令：UPDATE products SET stock = stock - 3 WHERE id = X;。至此，这 3 个库存才被永久扣除，车间正式开始为你打印 2D 模型和制作定制实物。💡 针对你自建站的高级开发技巧：如果你做的是“纯手工定制品”因为你一再强调你的产品是“定制品”，定制品和工厂流水线量产的标品（如手机、衣服）在库存逻辑上有一个巨大的技术红利：定制品通常是不需要设置“上限库存”的，或者说它是“虚拟无限库存”。标品痛点：仓库里只有 10 个手机，卖完就必须卡死接口，否则发不出货。定制品优势：只要你作为个人开发者，精力应付得过来，你的库存理论上是无限的。因为用户下一单，你就去车间/第三方平台做一单。你真正需要调接口限制的，是“每日接单上限（Capacity）”：例如，你一个人一天最多只能精修、制作 5 个定制实物。那么你在调用后端接口时，校验的不是商品的 physical stock（物理库存），而是校验“今天该类目已支付的订单总数是否达到了 5”。如果达到了，接口返回 今天定制名额已满，请明天再来。这种用代码控制“接单产能”的逻辑，远比天天去算购物车加减库存要轻松得多。请问在你的业务设想中，你的定制产品是由于手工制作导致每天只能做几个（产能限制），还是由某种特定原材料的数量决定了总共只能卖几个（原材料限制）？这两种情况在后端的 SQL 代码编写和 Redis 锁的设计上是完全不同的。


signupChannel 平台参数

1. input 输入框
2. 新增计数器设计


是这样这样像的，这个表单最后是要封装成一个组件的，我在想当组件渲染的时候先去判断有没有Google Places 的key, 有则用Google Places，没有则看，dr5hn jsDelivr有没有挂掉是否可访问的情况可以的话使用dr5hn，然后如果挂掉的话使用Photon ，这个期间是异步的可以使用骨架屏等待，这样组装成一个demo 可以吗


1. 样式问题，
2. 详细地址必填
3. 首次渲染会闪动展示所有的input


避坑补充（图片切图）：我们在前面提到的“把原图实时裁剪成缩略图”的 Cloudflare Images 服务超过每月 5,000 次后是收费的。如果你想一分钱不花，可以让前端工程师在打包或上架时，利用 Webpack/Vite 或者让后端在上传时直接生成好中、小两组图存在免费的存储里（如 Cloudflare R2 的 10GB 免费额度），直接通过免费的 CF CDN 分发，这样连图片切图费都能完全免掉！ 

操作方法：运营人员上传的原始主图，必须是 1:1（正方形）。并且，商品的实物主体只能占整个图片面积的 80% 左右，四周必须留出 10% 的空白安全区（Safe Area）。

独立站高级玩法 —— “智能焦点裁剪”（AI Content-Aware Crop）如果由于特殊原因，你们详情页中段的卖点大图必须在 PC 端展示长条形（16:9），在手机端展示竖条形（3:4），且必须撑满整个屏幕，同时还必须保证商品的“核心卖点”绝对不能被切掉。这时候就需要用到 Cloudflare 等高级 CDN 自带的 智能视觉焦点裁剪（Smart Cropping）：技术原理：你在 URL 后面不要写死从左边或中间裁剪，而是加上 gravity=auto 参数。Cloudflare 示例 URL：https://yourstore.com背后发生了什么：Cloudflare 的边缘节点在切图时，会自动运行一个轻量级的 AI 人脸/物体识别算法。它能自动识别出图片里“包包”或者“模特脸部”的位置，以此为核心圆心进行智能裁剪，把四周无意义的背景（如白墙、天空）切掉，确保核心商品永远在不同设备的视觉正中央。

商品主图/SKU图区域：前端组件、UI 设计稿、后台上传提示，全部焊死为 1:1（正方形）。前端写死 aspect-ratio: 1 / 1; object-fit: contain;。运营上传时统一用白底或纯色底，自带 10% 留白。这是最安全、转化率最高、跨端最丝滑、且完全不切图的方案。详情页中段落地页（Landing Page）长图文区：千万不要把几张不同比例的图拼成一个固定高度的框。直接让前端开发成“流式布局（Flow Layout）”，图片宽度给 width: 100%，高度给 height: auto。这样无论原图是横是竖，在手机和电脑上都会等比例放大缩小，100% 完整展现，绝对不会发生任何裁剪或变形。


您观察得非常仔细！这家海外独立站（No Football Colors）的产品主图确实不是传统的 1:1 正方形，而是采用了非常典型的 3:4（纵向长方形）竖图比例。这种“非 1:1 比例”在独立站，尤其是**时尚女装、潮流街头服饰（Streetwear）**品类中极为常见，而且完全不需要担心图片会被硬生生裁切。他们之所以能做到“完整显示且不裁剪”，是因为在开发和设计上做对了以下两件事：1. 他们全站焊死了统一的 3:4 “纵向比例骨架”裁剪的核心原因，是“原图比例”与“前端图片框比例”不一致（比如原图是长方形，前端硬要放进正方形框里）。这家网站在写前端 CSS 代码时，为所有图片的容器统一设置了完全一致的比例（Aspect Ratio）。同时，他们的摄影师在拍照或导出图片时，也是严格按照统一的 3:4 比例裁剪后才上传的。当“原图是 3:4” 且 “前端容器也是 3:4” 时，两者完全重合，自然不需要使用 object-fit: cover 去强行裁剪，整张图就能 100% 完美完整呈现。2. 为什么潮流服饰独立站极度偏爱 3:4 竖图？在开发阶段，如果你们主攻的也是服装、鞋帽或全身搭配（Lifestyle）类产品，3:4 的高转化优势非常明显：契合人体工学：衣服和模特都是纵向站立的。3:4 比例可以在不浪费左右两侧空间的前提下，完美展示出衣服的垂坠感、宽松版型（Oversized fit）以及整体的上下身搭配。撑满移动端屏幕：海外独立站 80% 的流量在手机上。3:4 的图片在手机端下滑浏览时，能刚好占据大半个屏幕，视觉冲击力远比 1:1 的正方形更强，更能刺激用户产生购买欲。🛠️ 针对你们独立站开发的终极建议你完全可以不选 1:1，选用和这家独立站一样的 3:4 比例！但在开发阶段，请让前端工程师在代码里写死这两个关键约束，否则后期只要上传一张尺寸不对的图，页面就会瞬间崩塌：css/* 针对服装/3:4 详情页组件的前端 CSS 开发规范 */
.product-main-image-container {
width: 100%;
/* 1. 强制容器的长宽比为 3:4 (相当于 width 3 份，height 4 份) */
aspect-ratio: 3 / 4;
overflow: hidden;
}

.product-main-image-container img {
width: 100%;
height: 100%;
/* 2. 降级防线：万一运营人员误传了一张正方形或横图 */
/* 使用 contain，配合白色或透明背景，宁可两边留白，也绝对不裁剪商品本体 */
object-fit: contain;
background-color: transparent;
}
请谨慎使用此类代码。💡 一句话总结：独立站高转化的核心不是必须用 1:1 还是 3:4，而是**“运营上传的比例”与“前端代码约束的比例”必须绝对统一**。请问你们目前规划售卖的具体产品品类是什么呢？是跟这家网站类似的服饰潮牌，还是数码配件、美妆或家居？如果是服饰类，那咱们现在就可以直接把视觉比例规范定死为 3:4 了！


1. DNS 级别的优化：Anycast DNS（任播技术）如果问题纯粹出在“DNS 解析”这一步（即用户电脑获取你网站 IP 地址的过程慢），你甚至不需要上 CDN，只需要换一个支持全球 Anycast 的智能 DNS 解析服务商。原理：Anycast 技术允许全球多个地点的 DNS 服务器使用同一个 IP 地址。海外用户发起 DNS 查询时，请求会被路由到离他最近的境外 DNS 节点。现成方案：如果你把域名的 NS（Name Servers）转到 Cloudflare（免费版即自带全球顶级 Anycast DNS）、Route 53 (AWS) 或 Dyn。海外用户的 DNS 解析时间能瞬间从几百毫秒降到 10-30 毫秒内。2. 网络传输层的优化：Anycast 路由网络（Anycast EIP / 统一加速通道）如果你的后台接口不能缓存（如前面聊到的 API），用 CDN 可能会因为频繁“回源”导致依然很慢。这时可以用网络层加速。原理：在海外用户家门口放一个 Anycast IP 作为入口，用户就近接入该公网 IP。然后，数据通过云厂商（如 AWS、阿里云、腾讯云）的跨境专用企业级内网专线直达你国内的服务器。优势：绕过了拥堵、丢包严重的公网国际公网出口。用户在本地就完成了 TCP/TLS 握手，之后的跨境传输在高速专线上跑。现成方案：AWS Global Accelerator（全球加速器）、阿里云 GA（全球加速）、腾讯云 GAAP。3. 应用层的优化：QUIC / HTTP3 协议传统的 HTTPS 需要经过 TCP 3次握手 + TLS 4次/2次握手，跨境网络下一旦丢包，握手就会卡死数十秒。原理：QUIC 协议（HTTP/3）基于 UDP。它将传输层和加密层握手合并。在理想情况下，可以做到 0-RTT（0次往返延迟） 就能开始发送数据。而且它具备“连接迁移”特性，手机切网络（如 Wi-Fi 切 5G）不需要重新握手。实现方式：在你的服务器（如 Nginx）上开启 HTTP/3 支持，或者通过 Cloudflare 免费版后台一键勾选开启 HTTP/3。


Vercel 免费版的两套规则普通静态图片缓存：完全免费 🆓如果你只是把图片放在项目的 public/ 文件夹下，或者作为静态资源打包。Vercel 会自动通过其内置的全球边缘网络（Edge Network / CDN）来缓存这些图片。这部分产生的所有出站流量，只要不超出免费版每月 100 GB 的总带宽限制，就是完全免费的。



计费维度对比：Vercel vs Cloudflare计费维度Vercel 免费版 (Hobby)Cloudflare 免费版 (Images Free)免费额度1,000 张 原图 / 月5,000 次 唯一裁剪（Transformations）/ 月计费核心只看“原图数量”。不管你裁剪多少种尺寸，1张原图就扣1个额度。看“唯一裁剪组合数量”。1张原图裁剪成 3 种尺寸，算 3 次额度。超额后果触发硬性限制。免费版无法加钱，超额后图片优化服务可能失效或报错。直接返回错误或回源。如果想继续用，需升级付费：$0.50 / 1,000次 裁剪。



为什么说 Cloudflare 的 5,000 次比 Vercel 的 1,000 张更耐用？虽然 Cloudflare 会把“不同尺寸”分开计费，但它的基础基数（5,000）是 Vercel 的 5 倍。在 Vercel 中：如果你网站上有 1,200 个商品，即使每个商品只有 1 张图，且只裁剪了 1 种尺寸，在 Vercel 也会直接超标。在 Cloudflare 中：这 1,200 张图如果用 <Image /> 统一裁剪成 width={300} 的一种尺寸，在 Cloudflare 里只消耗 1,200 次额度，离 5,000 的上限还绰绰有余，完全免费。💡 注意（同样不限访问次数）：不管是 Vercel 还是 Cloudflare，它们限制的都是“进入裁剪房的组合数”，而不是用户访问量。只要这 5,000 种裁剪组合被 Cloudflare 缓存住了，海外用户访问 100 万次也绝对不会多扣你一次额度。🚀 终极省钱方案：Astro 项目到底怎么搞？为了达成你最初的目标：彻底白嫖、最少成本、100% 解决跨境访问慢，你有两条最完美的路线可选：路线 A：使用 Cloudflare Pages 托管（完全抛弃 Vercel，最推荐）如果你的 Astro 项目只是展示型网站或外贸独立站，你甚至不需要用 Vercel。直接把 Astro 项目部署到 Cloudflare Pages。它同样完全免费，且不限制月流量（Vercel 限制 100GB）。在 Astro 中直接使用高级组件 <Image src="/product.png" width={300} />。只要你全站的“图片数 × 裁剪尺寸总数”在一个月内不超过 5,000 个，你的整个出海项目（托管 + 域名加速 + 图片裁剪）一分钱都不用花。路线 B：Vercel 托管 + 外层 Cloudflare 强行拦截（我们之前聊的方案）如果你非要用 Vercel 托管代码：不要在 Astro 里开启 Vercel 的图片优化（即保持 imageService 为默认或静态输出）。这样 Astro 会在打包时把所有图片直接生成为 public/ 下的普通静态文件。域名托管到 Cloudflare 并点亮橙色小云朵。所有的图片直接走 Cloudflare 的普通 CDN 缓存。这种情况下，既不扣 Vercel 的 1,000 额度，也不扣 Cloudflare 的 5,000 裁剪额度。因为图片没有经过任何动态裁剪 API，它们被当成了普通网页文件。这是真正意义上的“无限量白嫖”。如果你的图片在上传前已经用工具（如 TinyPNG）统一压缩过了，强烈建议选择路线 B，因为这种普通 CDN 缓存模式的额度是无限的，再大的网站也完全不用担心账单问题。目前你的 Astro 项目，所有的原图在上传前有经过人工压缩吗？还是希望全靠代码和云端自动帮你压缩和转 WebP 格式呢？



Cloudflare 不仅支持接收和转发所有的后台请求（如 API、表单提交），而且如果你使用 Astro 的 SSG（静态网站生成） 模式，Cloudflare 简直就是为你量身定做的。1. 发送后台请求：Cloudflare 怎么处理？对于登录、加购、留言表单等必须实时交互的“后台请求（API）”，你完全不用担心。全自动转发（不拦截）：Cloudflare 默认只缓存静态文件。当你的前端页面通过 fetch() 或 axios 向后台发送 POST 或动态 GET 请求时，Cloudflare 会化身为一个极速透明通道，自动把请求秒级转发给你的后端服务器。安全护航：它在转发的同时，还会顺手帮你拦截海外的恶意网络刷单、机器人爬虫和 DDoS 攻击，保护你的后台接口不被刷爆。2. Astro SSG 生成多个产品页 + Cloudflare（最强白嫖组合）你提到的“用 SSG 生成多个产品详情页”这个思路极其正确！在 Astro 中，这叫做 预渲染（Pre-rendering）。当你执行 npm run build 时，Astro 会调用你的后台接口拿到所有商品数据，然后一口气把所有的产品详情页编译成一堆纯 HTML 文件（例如 /products/tshirt/index.html）。把这套 SSG 架构放到 Cloudflare 上，会产生极其恐怖的性能与成本优势：🟢 优势一：图片和页面“彻底无限量免费”因为 SSG 在打包时，就已经把产品页和图片全部变成了静态文件。它们不属于动态接口，也不需要消耗任何“动态裁剪额度”。无论是托管在 Cloudflare Pages 还是 Vercel 上，它们都会被当成普通网页。结果：全球用户访问这几千个产品页和商品图，产生的流量是100% 免费且无限的。🟢 优势二：海外用户“无感秒开”因为产品页已经是纯 HTML 了，它们会直接被平铺到 Cloudflare 全球 300 多个海外边缘节点上。欧美、东南亚的用户点击产品详情页时，不需要等待你的后台数据库去查询，而是直接从他家门口的 CDN 节点秒吐出 HTML 页面。之前的“DNS 慢、TCP 握手慢”在静态 HTML 面前直接灰飞烟灭，网站速度可以轻松跑到 Google Lighthouse 的 95+ 高分。🟢 优势三：后台服务器压力降为 0传统模式：1万个用户看产品，你的后台就要查 1万次数据库，服务器很容易卡死。SSG 模式：你的后台服务器只需要在打包那一天（Build 时）接受一次 Astro 的批量请求，把数据吐给 Astro 即可。网页一旦生成好，后续千万次的海外用户访问全部由 Cloudflare 的免费节点替你抗下，你甚至可以买一个最便宜的国内低配服务器当后台。🛠️ 怎么在 Astro 落地这个方案？写好生成函数：在你的 Astro 产品详情页代码（如 src/pages/products/[id].astro）中，确保写了 export function getStaticPaths()。在这个函数里用 fetch 去拉取你的全部商品 ID。部署选择：如果选择 Cloudflare Pages：直接把 GitHub 仓库连过去，Astro 会把静态文件直接吐在 Cloudflare 的服务器上，无限流量白嫖到底。如果选择 Vercel：同样完全静态化，全站流量如果每个月在 100GB 以内，也是完全免费的。你目前后台里大概有多少个商品（或者产品详情页的数量）？需不需要我为你提供一段标准的支持 SSG 批量生成的 Astro 模板代码？

封装一个sku选择器
1. 添加属性可以配置货币字符|图标svg
2. title 是个插槽可以放标题元素的
3. 添加 change 事件每次修改sku要回调然后吧选中的sku数据和状态(选中的sku是否符合可以加购的状态，比如只选择weight,其他的并没有选择)传出来
4. 加购前要校验sku选中状态，提示信息直接在页面上提示就好了比如按钮的上面
5. 可以通过属性可以simple 开启的时候只会展示sku选择没有按钮和提示标题等
6. 要有submit事件回调 这个是这样的组件默认只有 ADD TO CART按钮, 价格也会展示到按钮里，会回调submit事件
回调是一个异步函数，回传参数是一个实例对象 instance.done() 之后取消按钮的loading和禁用状态
7. 如果是 simple 模式 submit 调用的时机就是选中的sku符合加购状态，然后loading状态可以放到最后点击的哪个sku按钮上，loading的时候整组sku都要禁用
8. 要适配移动端不同尺寸的设备，因为有可能会放到列表项中做快速加购

- 背景色什么的不要封装到组件里面，因为他默认只是个选择器，展示样式应该由外部决定

- sku算法 参考sku-selector-demo.html 但是绝对不要参考他的样式，只有它的算法逻辑
- sku样式参考地址 https://nofootballcolors.com/products/maillot-no-drug

1. 重新整理下 story  simple 模式，异步和同步等，快速加购